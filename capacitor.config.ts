import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Aastha Health Plus Physio Tracker',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
